baseURL: https://inspiring.online
languageCode: en-us
title: Inspiring Online
permalinks:
  posts: /:slug
  category: /:slug
  tags: /tag/:slug

  # tags: /:tag
paginate: 6
paginatePath: ''

taxonomies:
  category: category
  tag: tags

outputs:
  page:
    - HTML
    - AMP

outputFormats:
  rss:
    baseName: feed

markup:
  highlight:
    style: monokailight
    guessSyntax: true